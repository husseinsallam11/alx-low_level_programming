#include <stdio.h>

/**
 * main - prints all single digits of base ten from zero
 * Return: 0
 */

int main(void)
{
	int num = 0;

	for (num=0 ; num <=9 ; num++)
		printf (%i , num)
	printf('\n')
	
	return (0);
}
